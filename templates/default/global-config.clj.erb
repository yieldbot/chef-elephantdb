;; This configuration is stored globally on HDFS

{ :replication 2
  :hosts [<% @cluster_nodes.each do |edb_node| %>"<%= edb_node %>" <% end %>]
  :port 3578
  :domains {
    <% @domains.each do |domain| %>
      "<%= domain['id'] %>" "<%= @node['elephantdb']['dfs_path'] %>/<%= domain['id'] %>"
    <% end %>
  }
}
